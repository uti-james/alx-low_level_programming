#include "main.h"

/**
 * _isupper - checks for uppercase character
 * @c: character processed
 * Return: 1 if c is uppercase and 0 if otherwise
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
